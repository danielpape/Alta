var React = require('react');
var {Link} = require('react-router');

var About = (props)=>{
  return(
    <div>
      <h1 className="text-center page-title">About</h1>
      <p>Alta is a social network for weather and photography geeks.</p>
    </div>
  )
};

module.exports = About;
