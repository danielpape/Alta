var React = require('react');
var {Link} = require('react-router');

var Nearby = (props)=>{
  return(
    <div>
      <h1 className="text-center page-title">Nearby</h1>
    </div>
  )
};

module.exports = Nearby;
